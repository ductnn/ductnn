name: Update README
on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:
jobs:
  get-office-quotes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Let the magic happen
        uses: actions/setup-node@v1
        with:
          node-version: '15.x'

      - run: yarn

      - run: node .

      - name: Add to git repo
        run: |
          git config pull.rebase false
          git pull
          git add .
          git config --global user.email "trannhuduc51998@gmail.com"
          git config --global user.name "${GITHUB_ACTOR}"
          git commit -m "Updated README" || echo "No changes to commit"

      - name: Push
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

  # update-readme-with-blog:
  #   needs: get-office-quotes
  #   name: Update this repo's README with latest blog posts
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: gautamkrishnar/blog-post-workflow@master
        # with:
        #   # Replace this URL with your rss feed URL/s
        #   feed_list: "https://braydoncoyer.dev/rss.xml"
